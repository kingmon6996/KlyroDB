#include <gtest/gtest.h>
#include "klyro/execution/planner/planner.hpp"

using namespace klyro;
using namespace klyro::execution::planner;

TEST(PlannerTest, BasicCompilation) {
    ASSERT_TRUE(true);
}
