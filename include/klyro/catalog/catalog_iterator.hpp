#ifndef KLYRO_CATALOG_CATALOG_ITERATOR_HPP
#define KLYRO_CATALOG_CATALOG_ITERATOR_HPP

namespace klyro::catalog {
// To avoid exposing unordered_map iterators, we will just return std::vector copies from Catalog methods later
// if we need full iteration over the catalog objects.
}

#endif // KLYRO_CATALOG_CATALOG_ITERATOR_HPP
